'use strict';

/**
 * maintenance-format service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::maintenance-format.maintenance-format');
