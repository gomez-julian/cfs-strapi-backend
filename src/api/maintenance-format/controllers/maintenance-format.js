'use strict';

/**
 * maintenance-format controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::maintenance-format.maintenance-format');
