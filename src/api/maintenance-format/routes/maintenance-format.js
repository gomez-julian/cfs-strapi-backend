'use strict';

/**
 * maintenance-format router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::maintenance-format.maintenance-format');
