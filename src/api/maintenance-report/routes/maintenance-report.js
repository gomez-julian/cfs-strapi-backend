'use strict';

/**
 * maintenance-report router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::maintenance-report.maintenance-report');
