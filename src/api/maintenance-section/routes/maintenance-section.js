'use strict';

/**
 * maintenance-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::maintenance-section.maintenance-section');
